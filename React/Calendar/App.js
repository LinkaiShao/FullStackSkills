import React from 'react';
import Calendar from './calendar'; // Assuming Calendar component is in the same directory as the app component

const App = () => {
  return (
    <div>
      <h1>My Calendar App</h1>
      <Calendar />
    </div>
  );
};

export default App;
